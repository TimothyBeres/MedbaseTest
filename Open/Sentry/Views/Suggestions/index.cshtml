@using Open.Sentry1.Extensions
@model Open.Core.IPaginatedList<Open.Facade.Person.PersonViewModel>
@{
    //Layout = "_Layout";
    ViewBag.Title = "Soovitused";
    string word = "NB!";
    Layout = "_IndexPartial";
}

<h2>@ViewBag.Title</h2>

@*<form asp-action=@ViewBag.ActionIndex method="get">
        <div class="form-horizontal">
            <p>
                Search for patient (ID Code): <input type="text" name="SearchString" />
                <br />
                <button type="button" onclick="location.href='@Url.Action("PatientInfo","Suggestions", new { id_code=Model.IDCode})'">Search</button>
            </p>
        </div>
    </form>*@
@*<div class="text-danger" style="font-size: large">
    @ViewBag.Message
</div>

@Html.ValidationMessageFor(model => model.IDCode)
@using (Html.BeginForm("Patientinfo", "Suggestions", FormMethod.Post))
{

    @Html.TextBoxFor(model => model.IDCode)

    <button type="submit">Otsi</button><br />
    <br />
    <button type="button" onclick="location.href='@Url.Action("Create","Persons")'">Lisa patsient</button>
}*@
<body>
<table class="table">
    @{ var dummy = Model.FirstOrDefault();}
    <thead>
    <tr>
        @Html.SortColumnHeaderFor(ViewData["SortAlpha3"], model => dummy.IDCode)
        @Html.SortColumnHeaderFor(ViewData["SortAlpha2"], model => dummy.FirstName)
        @Html.SortColumnHeaderFor(ViewData["SortName"], model => dummy.LastName)
        @Html.SortColumnHeaderFor(ViewData["SortValidFrom"], model => dummy.ValidFrom)
        @Html.SortColumnHeaderFor(ViewData["SortValidTo"], model => dummy.ValidTo)
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.IDCode)</td>
            <td>@Html.DisplayFor(modelItem => item.FirstName)</td>
            <td>@Html.DisplayFor(modelItem => item.LastName)</td>
            <td>@Html.DisplayFor(modelItem => item.ValidFrom)</td>
            <td>@Html.DisplayFor(modelItem => item.ValidTo)</td>
            @Html.EditDetailDeleteFor(modelItem => item.ID)
        </tr>
    }
    </tbody>
</table>
</body>



